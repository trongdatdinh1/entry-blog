<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
  html: { method: :delete }, remote: true, class:"form-group") do |f| %>
  <%= f.submit "Unfollow", class:"btn btn-primary btn-lg" %>
<% end %>
