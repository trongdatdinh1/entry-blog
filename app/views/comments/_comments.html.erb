<% comments.each do |comm| %>
  <div class="card">
    <div class="card-body">
      <blockquote class="blockquote mb-0">
        <p><%= comm.text %>.</p>
        <footer class="blockquote-footer"><%= comm.user.name %></footer>
      </blockquote>
    </div>
  </div>
<% end %>
